// import db from '../../firebase/db';

// constants


// actions


// thunks


// reducer
const initialState = {
  team: 'Pending',
  clue: null,
  number: null,
};
export default function (prevState = initialState, action) {
  switch (action.type) {
    default:
      return prevState;
  }
}
